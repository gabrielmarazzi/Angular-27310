import { StiloDivDesaprobadoDirective } from './stilo-div-desaprobado.directive';

describe('StiloDivDesaprobadoDirective', () => {
  it('should create an instance', () => {
    const directive = new StiloDivDesaprobadoDirective();
    expect(directive).toBeTruthy();
  });
});

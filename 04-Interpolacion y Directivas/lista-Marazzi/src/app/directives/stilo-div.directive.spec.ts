import { StiloDivDirective } from './stilo-div.directive';

describe('StiloDivDirective', () => {
  it('should create an instance', () => {
    const directive = new StiloDivDirective();
    expect(directive).toBeTruthy();
  });
});
